version: '3'

services:
  polaris-backend:
    image: p2olab/polaris-backend:latest
    restart: always
    ports:
      - "3000:3000"
  polaris-frontend:
    image: p2olab/polaris-frontend:latest
    restart: always
    ports:
      - "80:80"
    depends_on:
      - polaris-backend
  mtp-converter:
    image: p2olab/mtp-converter:latest
    restart: always
    ports:
      - "3031:3031"
